[project]
name = "rc-gpt-image"
version = "0.1.0"
description = "Download asynchronous gpt-image-2 results from Right Code"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
package = false
